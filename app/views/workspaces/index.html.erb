<div id="workspace_index">
  <h1>All Workspaces with Free WiFi (By Top Rating)</h1>
  <ul>
    <% @workspaces.each do |workspace| %>
      <%= image_tag workspace.image_url %>
      <li><%= link_to workspace.name, workspace %></li>
      <li>Rating: <%= workspace.average_rating %></li>
      <li><%= workspace.address %></li>
      <li><%= workspace.category %></li>
      <li><%= link_to "Website", workspace.company_url %></li>
      <li><%= workspace.hours %></li>
    <% end %>
  </ul>
</div>
